import { AudioLines, Bot, ChevronLeft, ChevronRight, Image, MessageSquareMore, Moon, Orbit, Plus, Settings2, Sparkles, Sun, Trash2, Video } from "lucide-react";
import type { SessionOption } from "../types";
import { AppIcon } from "./AppIcon";

interface SidebarProps {
  pathname: string; sessions: SessionOption[]; selectedSession: string; isDark: boolean; isOpen: boolean; isCollapsed: boolean;
  onNavigate: (path: string) => void; onNewChat: () => void; onSelectSession: (id: string) => void; onDeleteSession: (session: SessionOption) => void; onToggleTheme: () => void; onClearVram: () => void; onClose: () => void; onToggleCollapse: () => void;
}

const nav = [
  { path: "/", label: "Chat", icon: MessageSquareMore, tone: "text-indigo-500" },
  { path: "/image", label: "Image studio", icon: Image, tone: "text-fuchsia-500" },
  { path: "/video", label: "Motion studio", icon: Video, tone: "text-rose-500" },
  { path: "/audio", label: "Sound lab", icon: AudioLines, tone: "text-cyan-500" },
];
const tools = [{ path: "/agents", label: "Agents & connections", icon: Bot }, { path: "/settings", label: "Settings", icon: Settings2 }];

export function Sidebar({ pathname, sessions, selectedSession, isDark, isOpen, isCollapsed, onNavigate, onNewChat, onSelectSession, onDeleteSession, onToggleTheme, onClearVram, onClose, onToggleCollapse }: SidebarProps) {
  const tooltip = (label: string) => isCollapsed ? { title: label, "aria-label": label } : { "aria-label": label };
  const itemClass = (active: boolean) => `group relative flex w-full items-center rounded-xl text-sm font-medium transition ${isCollapsed ? "justify-center px-2 py-2.5" : "gap-3 px-3 py-2.5"} ${active ? "bg-slate-950 text-white shadow-md shadow-slate-950/10 dark:bg-white dark:text-slate-950" : "text-slate-600 hover:bg-slate-100 dark:text-slate-300 dark:hover:bg-slate-900"}`;
  return <>{isOpen && <button aria-label="Close navigation" className="fixed inset-0 z-30 bg-slate-950/40 md:hidden" onClick={onClose} />}<aside className={`fixed inset-y-0 left-0 z-40 flex flex-col border-r border-slate-200/80 bg-white/90 p-3 backdrop-blur-xl transition-[width,transform] duration-300 dark:border-slate-800 dark:bg-slate-950/90 md:relative md:translate-x-0 ${isCollapsed ? "w-[76px]" : "w-72"} ${isOpen ? "translate-x-0" : "-translate-x-full"}`}>
    <div className={`flex items-center py-2 ${isCollapsed ? "justify-center" : "justify-between px-2"}`}><div className="flex items-center gap-3 overflow-hidden"><AppIcon />{!isCollapsed && <div className="whitespace-nowrap"><p className="font-bold tracking-tight text-slate-900 dark:text-white">Qwen Studio</p><p className="text-xs text-slate-500">Local AI workspace</p></div>}</div><button onClick={onToggleCollapse} className={`hidden rounded-lg p-1.5 text-slate-400 hover:bg-slate-100 hover:text-slate-700 dark:hover:bg-slate-900 dark:hover:text-slate-200 md:grid ${isCollapsed ? "absolute -right-3 top-5 size-6 place-items-center rounded-full border border-slate-200 bg-white shadow-sm dark:border-slate-700 dark:bg-slate-900" : ""}`} {...tooltip(isCollapsed ? "Expand sidebar" : "Collapse sidebar")}>{isCollapsed ? <ChevronRight size={14} /> : <ChevronLeft size={17} />}</button></div>
    <button onClick={onNewChat} className={`my-4 flex items-center justify-center gap-2 rounded-xl bg-slate-950 py-3 text-sm font-semibold text-white shadow-lg shadow-slate-950/10 transition hover:bg-indigo-600 dark:bg-white dark:text-slate-950 ${isCollapsed ? "px-2" : "px-4"}`} {...tooltip("New conversation")}><Plus size={18} />{!isCollapsed && "New conversation"}</button>
    <nav className="space-y-1">{nav.map(({ path, label, icon: Icon, tone }) => <button key={path} onClick={() => onNavigate(path)} className={itemClass(pathname === path)} {...tooltip(label)}><Icon size={18} className={pathname === path ? "" : tone} />{!isCollapsed && <span>{label}</span>}{pathname === path && !isCollapsed && <Sparkles size={13} className="ml-auto opacity-70" />}</button>)}<div className="my-3 border-t border-slate-100 dark:border-slate-800" />{tools.map(({ path, label, icon: Icon }) => <button key={path} onClick={() => onNavigate(path)} className={itemClass(pathname === path)} {...tooltip(label)}><Icon size={18} />{!isCollapsed && <span>{label}</span>}</button>)}</nav>
    {!isCollapsed && <div className="app-scrollbar mt-6 min-h-0 flex-1 overflow-y-auto pr-1"><p className="px-3 pb-2 text-[11px] font-bold uppercase tracking-wider text-slate-400">Recent conversations</p>{sessions.map((session) => <div key={session.id} className={`group flex items-center rounded-xl ${selectedSession === session.id ? "bg-slate-100 dark:bg-slate-900" : "hover:bg-slate-50 dark:hover:bg-slate-900/60"}`}><button onClick={() => onSelectSession(session.id)} className="min-w-0 flex-1 truncate px-3 py-2.5 text-left text-sm text-slate-600 dark:text-slate-300">{session.title}</button><button aria-label={`Delete ${session.title}`} onClick={() => onDeleteSession(session)} className="mr-1 hidden rounded-lg p-1.5 text-slate-400 hover:bg-red-50 hover:text-red-500 group-hover:block dark:hover:bg-red-950/30"><Trash2 size={14} /></button></div>)}</div>}
    {isCollapsed && <div className="flex-1" />}<div className="space-y-1 border-t border-slate-200 pt-3 dark:border-slate-800"><button onClick={onClearVram} className={`flex w-full items-center rounded-xl py-2.5 text-sm text-slate-500 hover:bg-red-50 hover:text-red-600 dark:hover:bg-red-950/30 ${isCollapsed ? "justify-center" : "gap-3 px-3"}`} {...tooltip("Clear VRAM")}><Trash2 size={17} />{!isCollapsed && "Clear VRAM"}</button><button onClick={onToggleTheme} className={`flex w-full items-center rounded-xl py-2.5 text-sm text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-900 ${isCollapsed ? "justify-center" : "gap-3 px-3"}`} {...tooltip(isDark ? "Light theme" : "Dark theme")}>{isDark ? <Sun size={17} /> : <Moon size={17} />}{!isCollapsed && (isDark ? "Light theme" : "Dark theme")}</button></div><div className={`mt-2 flex items-center text-[10px] font-medium text-emerald-600 dark:text-emerald-400 ${isCollapsed ? "justify-center" : "gap-1.5 px-3"}`} title="Local workspace online"><Orbit size={13} />{!isCollapsed && "Local workspace online"}</div>
  </aside></>;
}
