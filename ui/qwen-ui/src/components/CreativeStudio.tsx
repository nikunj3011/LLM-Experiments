import { Image, Mic2, SlidersHorizontal, Sparkles, Video } from "lucide-react";
import { useState } from "react";
import type { CreativeKind } from "../types";

const details = {
  image: { title: "Image canvas", description: "Turn a thought into an art-directed visual.", placeholder: "A chrome hummingbird navigating a midnight flower market, editorial lighting…", icon: Image, gradient: "from-cyan-400 via-violet-500 to-fuchsia-500", output: "1024 × 1024" },
  video: { title: "Motion studio", description: "Describe a scene, camera language, and the feeling you want.", placeholder: "Slow tracking shot through a sunlit brutalist gallery, soft grain, 24fps…", icon: Video, gradient: "from-amber-400 via-rose-500 to-violet-600", output: "6 sec · 1080p" },
  audio: { title: "Sound lab", description: "Make music, ambience, voice ideas, and sonic worlds.", placeholder: "Warm modular synth loop for a rainy train ride, 98 BPM, tape texture…", icon: Mic2, gradient: "from-emerald-400 via-cyan-500 to-blue-600", output: "0:30 · stereo" },
} as const;

export function CreativeStudio({ kind, onCreate }: { kind: CreativeKind; onCreate: (prompt: string) => void }) {
  const [prompt, setPrompt] = useState("");
  const [wide, setWide] = useState(kind === "video");
  const config = details[kind];
  const Icon = config.icon;
  return <section className="app-scrollbar min-h-0 flex-1 overflow-y-auto px-4 py-6 sm:px-8"><div className="mx-auto max-w-6xl">
    <div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><div className={`mb-3 grid size-11 place-items-center rounded-2xl bg-gradient-to-br ${config.gradient} text-white shadow-lg`}><Icon size={21} /></div><p className="text-xs font-bold uppercase tracking-[.18em] text-indigo-500">Creative workspace</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-slate-950 dark:text-white">{config.title}</h1><p className="mt-1 text-slate-500">{config.description}</p></div><div className="rounded-2xl border border-slate-200 bg-white px-4 py-3 text-right shadow-sm dark:border-slate-800 dark:bg-slate-900"><p className="text-[10px] font-bold uppercase tracking-wider text-slate-400">Output</p><p className="text-sm font-semibold text-slate-700 dark:text-slate-200">{config.output}</p></div></div>
    <div className="grid gap-5 lg:grid-cols-[1.25fr_.75fr]"><div className={`relative min-h-[410px] overflow-hidden rounded-[28px] bg-gradient-to-br ${config.gradient} p-px shadow-2xl shadow-indigo-500/20`}><div className="flex h-full min-h-[408px] flex-col justify-between rounded-[27px] bg-slate-950/90 p-6 text-white"><div className="flex items-center justify-between"><span className="rounded-full border border-white/15 bg-white/10 px-3 py-1 text-xs font-semibold backdrop-blur">Live concept</span><span className="flex items-center gap-1.5 text-xs text-white/65"><span className="size-2 rounded-full bg-emerald-400" /> Local queue ready</span></div><div className="max-w-md"><div className="mb-5 grid size-14 place-items-center rounded-2xl bg-white/10 backdrop-blur"><Sparkles size={25} /></div><h2 className="text-3xl font-semibold tracking-tight text-white">Create with intent,<br />not prompts alone.</h2><p className="mt-3 max-w-sm text-sm leading-6 text-white/65">Set the mood, reference, and output controls—then send it to your local creative pipeline.</p></div><div className="flex gap-2"><span className="rounded-lg bg-white/10 px-2.5 py-1 text-xs text-white/75">Style aware</span><span className="rounded-lg bg-white/10 px-2.5 py-1 text-xs text-white/75">Private by default</span></div></div></div>
      <div className="space-y-4 rounded-[28px] border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div className="flex items-center justify-between"><p className="font-semibold text-slate-800 dark:text-slate-100">Direction</p><button className="rounded-lg p-2 text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800" aria-label="Generation controls"><SlidersHorizontal size={17} /></button></div><textarea value={prompt} onChange={(e) => setPrompt(e.target.value)} className="h-32 w-full resize-none rounded-2xl border border-slate-200 bg-slate-50 p-3 text-sm leading-6 outline-none transition focus:border-indigo-400 focus:ring-4 focus:ring-indigo-500/10 dark:border-slate-700 dark:bg-slate-950" placeholder={config.placeholder} /><div className="flex items-center justify-between"><button onClick={() => setWide(!wide)} className="rounded-xl border border-slate-200 px-3 py-2 text-xs font-semibold text-slate-600 dark:border-slate-700 dark:text-slate-300">{wide ? "16:9 widescreen" : "1:1 square"}</button><button disabled={!prompt.trim()} onClick={() => onCreate(prompt)} className="inline-flex items-center gap-2 rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-semibold text-white transition hover:bg-indigo-600 disabled:cursor-not-allowed disabled:opacity-40 dark:bg-white dark:text-slate-950"><Sparkles size={16} /> Create</button></div><p className="text-xs leading-5 text-slate-400">Creates a new task in chat with your selected local model. Add a ComfyUI workflow to render assets into the gallery.</p></div></div>
  </div></section>;
}
