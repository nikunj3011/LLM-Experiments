import { ChevronDown, Menu, Plus, Radio } from "lucide-react";
import type { ModelOption } from "../types";
import { AppIcon } from "./AppIcon";

interface HeaderProps { models: ModelOption[]; selectedModel: string; isOpen: boolean; title: string; onModelChange: (id: string) => void; onToggleModels: () => void; onMenu: () => void; onNewChat: () => void; }

export function Header({ models, selectedModel, isOpen, title, onModelChange, onToggleModels, onMenu, onNewChat }: HeaderProps) {
  const selected = models.find((model) => model.id === selectedModel);
  return <header className="sticky top-0 z-20 flex min-h-[76px] items-center justify-between gap-3 border-b border-slate-200/70 bg-white/85 px-4 py-3 backdrop-blur-xl dark:border-slate-800 dark:bg-slate-950/85 sm:px-6"><div className="flex min-w-0 items-center gap-3"><button aria-label="Open navigation" onClick={onMenu} className="rounded-xl p-2 text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-900 md:hidden"><Menu size={20} /></button><div className="hidden items-center gap-2 sm:flex"><AppIcon /><div><div className="flex items-center gap-2"><p className="text-sm font-bold text-slate-900 dark:text-white">Qwen Studio</p><span className="rounded-md bg-indigo-50 px-1.5 py-0.5 text-[10px] font-bold uppercase tracking-wider text-indigo-600 dark:bg-indigo-500/15 dark:text-indigo-300">{title}</span></div><p className="flex items-center gap-1 text-xs text-emerald-600 dark:text-emerald-400"><Radio size={11} /> Local workspace</p></div></div><div className="relative"><button onClick={onToggleModels} aria-haspopup="listbox" aria-expanded={isOpen} className="flex h-12 min-w-[210px] max-w-[300px] items-center gap-2.5 rounded-2xl border border-slate-200 bg-white px-3 text-left shadow-sm transition hover:border-indigo-300 hover:shadow-md dark:border-slate-700 dark:bg-slate-900 sm:min-w-[260px]"><div className="grid size-8 shrink-0 place-items-center rounded-xl bg-gradient-to-br from-indigo-500 to-violet-600 text-xs font-bold text-white">AI</div><div className="min-w-0 flex-1"><p className="text-[10px] font-bold uppercase tracking-wider text-slate-400">Active model</p><p className="truncate text-sm font-semibold text-slate-700 dark:text-slate-100">{selected?.name ?? "Select model"}</p></div><ChevronDown size={17} className={`shrink-0 text-slate-400 transition ${isOpen ? "rotate-180" : ""}`} /></button>{isOpen && <div role="listbox" className="absolute left-0 top-full z-30 mt-2 w-full min-w-[280px] rounded-2xl border border-slate-200 bg-white p-1.5 shadow-2xl dark:border-slate-700 dark:bg-slate-900">{models.map((model) => <button role="option" aria-selected={model.id === selectedModel} key={model.id} onClick={() => onModelChange(model.id)} className={`flex w-full flex-col rounded-xl px-3 py-2.5 text-left hover:bg-slate-100 dark:hover:bg-slate-800 ${model.id === selectedModel ? "bg-indigo-50 text-indigo-700 dark:bg-indigo-500/15 dark:text-indigo-300" : ""}`}><span className="text-sm font-semibold">{model.name}</span><span className="text-xs opacity-60">{model.id}</span></button>)}</div>}</div></div><button onClick={onNewChat} className="inline-flex h-11 shrink-0 items-center gap-2 rounded-xl bg-slate-950 px-3.5 text-sm font-semibold text-white shadow-sm transition hover:bg-indigo-600 dark:bg-white dark:text-slate-950"><Plus size={17} /><span className="hidden sm:inline">New chat</span></button></header>;
}
