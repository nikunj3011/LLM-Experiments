import { Brain, Paperclip, Send, X, Zap } from "lucide-react";
import { useRef } from "react";
import type { GenerationMode } from "../types";

interface Props { prompt: string; file: File | null; mode: GenerationMode; isGenerating: boolean; onPromptChange: (value: string) => void; onFileChange: (file: File | null) => void; onModeChange: (mode: GenerationMode) => void; onSubmit: () => void; }
export function ChatComposer({ prompt, file, mode, isGenerating, onPromptChange, onFileChange, onModeChange, onSubmit }: Props) {
  const inputRef = useRef<HTMLInputElement>(null);
  return <div className="sticky bottom-0 bg-gradient-to-t from-slate-50 via-slate-50 to-transparent px-4 pb-5 pt-10 dark:from-slate-950 dark:via-slate-950"><div className="mx-auto max-w-4xl rounded-2xl border border-slate-200 bg-white p-2 shadow-xl shadow-slate-200/40 dark:border-slate-700 dark:bg-slate-900 dark:shadow-black/20">{file && <div className="mb-2 flex items-center justify-between rounded-xl bg-indigo-50 px-3 py-2 text-sm text-indigo-700 dark:bg-indigo-500/15 dark:text-indigo-300"><span className="truncate">{file.name}</span><button onClick={() => onFileChange(null)}><X size={16} /></button></div>}<textarea value={prompt} onChange={(event) => onPromptChange(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); onSubmit(); } }} placeholder={mode === "thinking" ? "Ask for a considered answer…" : "Message Qwen…"} rows={1} className="max-h-44 min-h-12 w-full resize-none bg-transparent px-3 py-2 text-[15px] outline-none" /><div className="flex items-center justify-between gap-2"><div className="flex items-center gap-1"><input ref={inputRef} type="file" className="hidden" onChange={(event) => onFileChange(event.target.files?.[0] ?? null)} /><button onClick={() => inputRef.current?.click()} className="rounded-xl p-2 text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-800" aria-label="Attach file"><Paperclip size={18} /></button><button onClick={() => onModeChange("flash")} className={`rounded-lg px-2 py-1.5 text-xs font-semibold ${mode === "flash" ? "bg-indigo-50 text-indigo-700 dark:bg-indigo-500/15 dark:text-indigo-300" : "text-slate-500"}`}><Zap className="mr-1 inline" size={13} />Flash</button><button onClick={() => onModeChange("thinking")} className={`rounded-lg px-2 py-1.5 text-xs font-semibold ${mode === "thinking" ? "bg-violet-50 text-violet-700 dark:bg-violet-500/15 dark:text-violet-300" : "text-slate-500"}`}><Brain className="mr-1 inline" size={13} />Think</button></div><button onClick={onSubmit} disabled={isGenerating || (!prompt.trim() && !file)} className="grid size-10 place-items-center rounded-xl bg-slate-950 text-white transition hover:bg-indigo-600 disabled:opacity-40 dark:bg-white dark:text-slate-950"><Send size={17} /></button></div></div></div>;
}
