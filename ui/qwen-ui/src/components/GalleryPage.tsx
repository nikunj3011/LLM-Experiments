import { Image as ImageIcon, RefreshCw, Video } from "lucide-react";
import type { GalleryAsset } from "../types";

export function GalleryPage({ kind, assets, isLoading, onRefresh }: { kind: "image" | "video"; assets: GalleryAsset[]; isLoading: boolean; onRefresh: () => void }) {
  const isImage = kind === "image";
  return <section className="mx-auto w-full max-w-6xl p-5 sm:p-8"><div className="mb-8 flex items-end justify-between"><div><div className="mb-3 grid size-11 place-items-center rounded-2xl bg-indigo-100 text-indigo-600 dark:bg-indigo-500/15 dark:text-indigo-300">{isImage ? <ImageIcon /> : <Video />}</div><h1 className="text-3xl font-bold tracking-tight text-slate-900 dark:text-white">Generated {isImage ? "images" : "videos"}</h1><p className="mt-1 text-slate-500">Your latest local creations in one place.</p></div><button onClick={onRefresh} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2 text-sm font-medium text-slate-600 hover:bg-white dark:border-slate-700 dark:text-slate-300 dark:hover:bg-slate-900"><RefreshCw size={15} className={isLoading ? "animate-spin" : ""} /> Refresh</button></div>{isLoading ? <div className="grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">{Array.from({ length: 8 }, (_, index) => <div key={index} className="aspect-square animate-pulse rounded-2xl bg-slate-200 dark:bg-slate-800" />)}</div> : assets.length ? <div className={isImage ? "grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4" : "grid gap-5 md:grid-cols-2 lg:grid-cols-3"}>{assets.map((asset) => <figure key={asset.url} className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl dark:border-slate-800 dark:bg-slate-900">{isImage ? <img src={asset.url} alt={asset.filename} loading="lazy" className="aspect-square w-full object-cover" /> : <video src={asset.url} controls loop className="aspect-video w-full bg-black object-cover" />}<figcaption className="truncate px-3 py-2.5 text-xs text-slate-500">{asset.filename}</figcaption></figure>)}</div> : <div className="grid min-h-72 place-items-center rounded-3xl border border-dashed border-slate-300 bg-white/50 text-center dark:border-slate-700 dark:bg-slate-900/40"><div><p className="font-semibold text-slate-700 dark:text-slate-200">Nothing here yet</p><p className="mt-1 text-sm text-slate-500">Run a ComfyUI {kind} workflow and refresh this page.</p></div></div>}</section>;
}
